import {Navigate,Route,Routes} from 'react-router-dom'; import {LandingPage} from '../pages/LandingPage'; import {ArchiveOverview} from '../pages/ArchiveOverview'; import {PlaceholderPage} from '../pages/PlaceholderPage'; import {Shell} from '../components/Shell';
export function App(){return <Routes><Route path="/" element={<LandingPage/>}/><Route element={<Shell/>}><Route path="/archive" element={<ArchiveOverview/>}/>{['home','profile','messages','photos','friends','memories','search'].map(p=><Route key={p} path={'/'+p} element={<PlaceholderPage title={p}/>}/>)}</Route><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
