import {fireEvent,render,screen} from '@testing-library/react'; import {MemoryRouter} from 'react-router-dom'; import {LandingPage} from './LandingPage';
describe('Landing page',()=>{it('rejects non-ZIP files and accepts ZIPs',()=>{render(<MemoryRouter><LandingPage/></MemoryRouter>);const input=document.querySelector('input[type=file]') as HTMLInputElement;fireEvent.change(input,{target:{files:[new File(['nope'],'notes.txt',{type:'text/plain'})]}});expect(screen.getByRole('alert')).toHaveTextContent('Choose a ZIP');fireEvent.change(input,{target:{files:[new File(['zip'],'archive.zip',{type:'application/zip'})]}});expect(screen.getByText('archive.zip')).toBeInTheDocument();expect(screen.getByRole('button',{name:/Inspect archive/})).toBeEnabled()})});
