import{useEffect,useState}from'react';import{ChevronDown,Clock3,FileJson,Image as ImageIcon,LoaderCircle,SlidersHorizontal}from'lucide-react';import{database}from'../database/client';import type{Page}from'../database/types';import type{Post}from'../archive/schemas/models';import{useDatabaseQuery}from'./useDatabaseQuery';
const PAGE_SIZE=20;const shownDate=(value?:string)=>value?new Date(value).toLocaleString():'Date unavailable in archive';
export function PostsPage(){const[sort,setSort]=useState<'newest'|'oldest'>('newest'),[year,setYear]=useState<number>(),[page,setPage]=useState<Page<Post>>({items:[],hasMore:false}),[loading,setLoading]=useState(true),[loadingMore,setLoadingMore]=useState(false),[error,setError]=useState('');const{data:stats}=useDatabaseQuery(()=>database.stats());useEffect(()=>{let active=true;setLoading(true);setError('');database.posts({limit:PAGE_SIZE,sort,year}).then(next=>active&&setPage(next)).catch(caught=>active&&setError(caught instanceof Error?caught.message:String(caught))).finally(()=>active&&setLoading(false));return()=>{active=false}},[sort,year]);const loadMore=()=>{if(!page.nextCursor||loadingMore)return;setLoadingMore(true);database.posts({limit:PAGE_SIZE,sort,year,cursor:page.nextCursor}).then(next=>setPage(current=>({...next,items:[...current.items,...next.items]}))).catch(caught=>setError(caught instanceof Error?caught.message:String(caught))).finally(()=>setLoadingMore(false))};return <div className="page"><div className="page-heading-row"><div><p className="eyebrow">Local timeline</p><h1 className="section-title">Posts</h1><p className="section-lead">Paginated normalized text posts. Reactions and comments are not imported yet.</p></div><div className="filter-row"><label><SlidersHorizontal/> Order<select value={sort} onChange={event=>setSort(event.target.value as 'newest'|'oldest')}><option value="newest">Newest first</option><option value="oldest">Oldest first</option></select><ChevronDown/></label><label>Year<select aria-label="Filter by year" value={year??''} onChange={event=>setYear(event.target.value?Number(event.target.value):undefined)}><option value="">All years</option>{stats?.years.map(value=><option key={value} value={value}>{value}</option>)}</select><ChevronDown/></label></div></div>{loading?<p className="empty"><LoaderCircle className="spin"/> Loading posts from SQLite…</p>:error?<p className="error">{error}</p>:!page.items.length?<p className="empty">No supported text posts match this filter.</p>:<><div className="post-list">{page.items.map(post=><article key={post.id}>{post.title&&<h2>{post.title}</h2>}<p>{post.text||'This post has no text content.'}</p><footer><span><Clock3/> {shownDate(post.createdAt)}</span>{post.mediaCount?<span><ImageIcon/> {post.mediaCount} media reference{post.mediaCount===1?'':'s'}</span>:null}<span title={post.source.path}><FileJson/> {post.source.path.split('/').pop()}</span></footer></article>)}</div>{page.hasMore&&<button className="load-more" onClick={loadMore} disabled={loadingMore}>{loadingMore?<><LoaderCircle className="spin"/> Loading more…</>:'Load older posts'}</button>}</>}</div>}
