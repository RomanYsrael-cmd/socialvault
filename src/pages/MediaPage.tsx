import{useEffect,useState}from'react';import{FileJson,Image as ImageIcon,LoaderCircle,Music,Paperclip,Video}from'lucide-react';import{database}from'../database/client';import type{Media}from'../archive/schemas/models';import type{Page}from'../database/types';
const icon:Record<Media['mediaType'],typeof ImageIcon>={photo:ImageIcon,video:Video,audio:Music,file:Paperclip,unknown:Paperclip};const date=(value?:string)=>value?new Date(value).toLocaleString():'Date unavailable';
export function MediaPage(){const[page,setPage]=useState<Page<Media>>({items:[],hasMore:false}),[loading,setLoading]=useState(true),[loadingMore,setLoadingMore]=useState(false),[error,setError]=useState('');useEffect(()=>{let active=true;database.media({limit:30}).then(next=>active&&setPage(next)).catch(caught=>active&&setError(caught instanceof Error?caught.message:String(caught))).finally(()=>active&&setLoading(false));return()=>{active=false}},[]);const loadMore=()=>{if(!page.nextCursor||loadingMore)return;setLoadingMore(true);database.media({limit:30,cursor:page.nextCursor}).then(next=>setPage(current=>({...next,items:[...current.items,...next.items]}))).catch(caught=>setError(caught instanceof Error?caught.message:String(caught))).finally(()=>setLoadingMore(false))};return <div className="page"><p className="eyebrow">Metadata only</p><h1 className="section-title">Photos & media</h1><p className="section-lead">These are indexed references from your archive. SocialVault does not extract or copy binary media yet.</p>{loading?<p className="empty"><LoaderCircle className="spin"/> Loading media metadata…</p>:error?<p className="error">{error}</p>:!page.items.length?<p className="empty">No media metadata was found in supported posts or messages.</p>:<><div className="media-list">{page.items.map(item=>{const MediaIcon=icon[item.mediaType];return <article key={item.id}><span className="media-icon"><MediaIcon/></span><span><b>{item.filename||item.path.split('/').pop()||'Unnamed media'}</b><small>{item.mediaType}{item.mimeType?` · ${item.mimeType}`:''} · {date(item.timestamp)}</small>{item.caption&&<p>{item.caption}</p>}<small title={item.source.path}><FileJson/> {item.source.path.split('/').pop()}</small></span></article>})}</div>{page.hasMore&&<button className="load-more" onClick={loadMore} disabled={loadingMore}>{loadingMore?<><LoaderCircle className="spin"/> Loading more…</>:'Load more media'}</button>}</>}</div>}
