import{UserRound,AtSign,CalendarDays,FileJson}from'lucide-react';import{database}from'../database/client';import{useDatabaseQuery}from'./useDatabaseQuery';
export function ProfilePage(){const{data:p,loading,error}=useDatabaseQuery(()=>database.profile());return <div className="page"><p className="eyebrow">Normalized archive</p><h1 className="section-title">Profile</h1>{loading?<p className="empty">Loading local profile…</p>:error?<p className="error">{error}</p>:!p?<p className="empty">No supported profile metadata was imported.</p>:<section className="profile-card"><div className="avatar"><UserRound/></div><div><h2>{p.displayName}</h2>{p.username&&<p><AtSign/> {p.username}</p>}{p.joinedAt&&<p><CalendarDays/> Joined {new Date(p.joinedAt).toLocaleDateString()}</p>}{p.bio&&<blockquote>{p.bio}</blockquote>}<small><FileJson/> Source: {p.source.path}</small></div></section>}</div>}
