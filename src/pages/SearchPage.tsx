import{useEffect,useState}from'react';import{Clock3,FileJson,LoaderCircle,MessageCircle,MessagesSquare,Search as SearchIcon,UserRound}from'lucide-react';import{Link}from'react-router-dom';import{database}from'../database/client';import type{SearchResponse,SearchResult}from'../database/types';
const labels:Record<string,string>={post:'Posts',message:'Messages',conversation:'Conversations',person:'People'};const icons:Record<string,typeof FileJson>={post:FileJson,message:MessageCircle,conversation:MessagesSquare,person:UserRound};const date=(value?:string)=>value?new Date(value).toLocaleString():'';const blank:SearchResponse={items:[],counts:{},hasMore:false,backend:'fts5'};
export function SearchPage(){const[query,setQuery]=useState(''),[applied,setApplied]=useState(''),[result,setResult]=useState<SearchResponse>(blank),[loading,setLoading]=useState(false),[loadingMore,setLoadingMore]=useState(false),[error,setError]=useState('');useEffect(()=>{const timer=window.setTimeout(()=>setApplied(query.trim()),300);return()=>window.clearTimeout(timer)},[query]);useEffect(()=>{let active=true;if(!applied){setResult(blank);setLoading(false);return}setLoading(true);setError('');database.search(applied,{limit:30}).then(next=>active&&setResult(next)).catch(caught=>active&&setError(caught instanceof Error?caught.message:String(caught))).finally(()=>active&&setLoading(false));return()=>{active=false}},[applied]);const loadMore=()=>{if(!result.nextCursor||loadingMore)return;setLoadingMore(true);database.search(applied,{limit:30,cursor:result.nextCursor}).then(next=>setResult(current=>({...next,items:[...current.items,...next.items],counts:Object.entries({...current.counts,...next.counts}).reduce<Record<string,number>>((counts,[type])=>{counts[type]=(current.counts[type]??0)+(next.counts[type]??0);return counts}, {})}))).catch(caught=>setError(caught instanceof Error?caught.message:String(caught))).finally(()=>setLoadingMore(false))};const groups=(Object.keys(labels)).map(type=>({type,items:result.items.filter(item=>item.entityType===type)})).filter(group=>group.items.length);const destination=(item:SearchResult)=>item.entityType==='post'?'/home':item.entityType==='message'&&item.conversationId?`/messages/${encodeURIComponent(item.conversationId)}?q=${encodeURIComponent(applied)}`:item.entityType==='conversation'?`/messages/${encodeURIComponent(item.entityId)}`:'/profile';return <div className="page"><p className="eyebrow">Local archive index</p><h1 className="section-title">Search</h1><p className="section-lead">Search runs inside SQLite FTS5 when available; archive contents stay on this device.</p><label className="global-search"><SearchIcon/><input autoFocus value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search posts, messages, people…" aria-label="Search archive"/></label>{loading?<p className="empty"><LoaderCircle className="spin"/> Searching locally…</p>:error?<p className="error">{error}</p>:!applied?<p className="empty search-hint">Type a word or phrase to search your imported archive.</p>:!groups.length?<p className="empty">No results for “{applied}”.</p>:<><div className="search-results">{groups.map(group=>{const GroupIcon=icons[group.type]??FileJson;return <section className="search-group" key={group.type}><div className="group-heading"><h2>{labels[group.type]??group.type}</h2><span>{result.counts[group.type]??group.items.length}</span></div>{group.items.map(item=><Link className="search-result" key={`${item.entityType}:${item.entityId}`} to={destination(item)}><span className="result-icon"><GroupIcon/></span><span><b>{item.title||labels[item.entityType]||item.entityType}</b><strong>{item.body||item.context||'No text preview'}</strong><small>{item.context&&item.body?item.context:''}{item.createdAt&&<><Clock3/> {date(item.createdAt)}</>}<FileJson/> {item.sourcePath?.split('/').pop()}</small></span></Link>)}</section>})}</div>{result.hasMore&&<button className="load-more" onClick={loadMore} disabled={loadingMore}>{loadingMore?<><LoaderCircle className="spin"/> Loading more results…</>:'Load more results'}</button>}</>}</div>}
