/**
 * Facebook adapter compatibility version. Keep this independent from the
 * application package version so normalized data can be evaluated safely
 * after a parser-only upgrade.
 */
export const FACEBOOK_PARSER_VERSION = 9;
