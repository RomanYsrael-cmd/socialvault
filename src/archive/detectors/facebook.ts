import type {ArchiveDetector,ArchiveEntryInfo,DetectionResult} from './types';
const markers=[['profile_information','Profile'],['posts','Posts'],['messages','Messages'],['photos_and_videos','Photos'],['friends_and_followers','Friends'],['likes_and_reactions','Reactions'],['comments','Comments'],['your_activity_across_facebook','Activity'],['personal_information','Profile'] ] as const;
export const facebookDetector:ArchiveDetector={platform:'facebook',detect(entries:ArchiveEntryInfo[]):DetectionResult{const paths=entries.map(e=>e.filename.toLowerCase().replaceAll('\\','/'));const jsonCount=paths.filter(p=>p.endsWith('.json')).length;const sections=[...new Set(markers.filter(([m])=>paths.some(p=>p.includes(m))).map(([,s])=>s))];const identity=paths.some(p=>p.includes('profile_information/profile_information.json')||p.endsWith('/profile_information.json'));const structure=paths.some(p=>p.startsWith('your_facebook_activity/')||p.startsWith('personal_information/'))||sections.length>=2;const supported=jsonCount>0&&(identity||structure);return {supported,platform:supported?'facebook':'unknown',confidence:supported?Math.min(.98,.55+sections.length*.08):.05,entryCount:entries.length,inspectedEntries:entries.length,sections,warnings:supported&&sections.length<2?['Only a limited set of recognizable Facebook sections was found.']:[]}}};
