import {describe,expect,it} from 'vitest'; import {detectArchive,detectArchiveSet} from '.';
describe('Facebook archive detector',()=>{it('detects a plausible JSON archive',()=>{const r=detectArchive([{filename:'profile_information/profile_information.json',directory:false},{filename:'your_facebook_activity/posts/your_posts__1.json',directory:false},{filename:'messages/inbox/chat/message_1.json',directory:false}]);expect(r.supported).toBe(true);expect(r.platform).toBe('facebook');expect(r.format).toBe('json');expect(r.sections).toContain('Messages')});it('recognizes profile_v2 naming and filtered Messenger paths',()=>{const r=detectArchive([{filename:'profile_v2/profile_v2.json',directory:false},{filename:'messages/filtered_messages/chat/message-1.json',directory:false}]);expect(r.supported).toBe(true);expect(r.sections).toContain('Profile')});it('aggregates partial archive parts',()=>{const r=detectArchiveSet([[{filename:'profile_information/profile_information.json',directory:false}],[{filename:'messages/inbox/chat/message_1.json',directory:false}]]);expect(r.supported).toBe(true);expect(r.sections).toEqual(expect.arrayContaining(['Profile','Messages']))});it('recognizes but does not accept an HTML-only export',()=>{const entries=[{filename:'personal_information/profile_information/profile_information.html',directory:false},{filename:'your_facebook_activity/posts/your_posts_1.html',directory:false},{filename:'your_facebook_activity/messages/inbox/thread/message_1.html',directory:false}];const r=detectArchive(entries);expect(r.supported).toBe(false);expect(r.platform).toBe('unknown');expect(r.format).toBe('html');expect(r.sections).toEqual(expect.arrayContaining(['Profile','Posts','Messages']));expect(r.warnings.join(' ')).toMatch(/HTML export/);expect(detectArchiveSet([entries]).supported).toBe(false)});it('does not label unrelated HTML as Facebook structure',()=>{const r=detectArchive([{filename:'documents/readme.html',directory:false}]);expect(r.supported).toBe(false);expect(r.format).toBe('html');expect(r.warnings.join(' ')).not.toMatch(/HTML export/)});it('rejects unrelated ZIP contents',()=>{expect(detectArchive([{filename:'documents/tax.pdf',directory:false}]).supported).toBe(false)});it('rejects suspicious paths',()=>{expect(()=>detectArchive([{filename:'../../escape.json',directory:false}])).toThrow(/Unsafe archive path/)})});
