import {Archive,Home,User,MessageCircle,Image,Users,History,Search,Menu} from 'lucide-react'; import {NavLink,Outlet} from 'react-router-dom'; import {Logo} from './Logo';
const items=[['Home',Home],['Profile',User],['Messages',MessageCircle],['Photos',Image],['People',Users],['Memories',History],['Search',Search],['Archive',Archive]] as const;
export function Shell(){return <div className="shell"><header><Logo/><button className="icon-button" aria-label="Open menu"><Menu/></button></header><aside><Logo/><nav>{items.map(([label,Icon])=><NavLink key={label} to={'/'+label.toLowerCase()}><Icon size={20}/><span>{label}</span></NavLink>)}</nav><div className="local-badge"><span>●</span><div><b>Local archive</b><small>Nothing uploaded</small></div></div></aside><main><Outlet/></main></div>}
