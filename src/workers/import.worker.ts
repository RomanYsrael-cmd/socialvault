/// <reference lib="webworker" />
import{BlobReader,TextWriter,ZipReader}from'@zip.js/zip.js';import{detectArchive}from'../archive/detectors';import{emptyNormalizedData,parseFacebookConversation,parseFacebookPosts,parseFacebookProfile}from'../archive/adapters/facebook-parser';import type{ImportProgress,ImportRequest}from'./protocol';
const send=(message:ImportProgress)=>postMessage(message);
self.onmessage=async(event:MessageEvent<File|ImportRequest>)=>{let reader:ZipReader<Blob>|undefined;try{const file=event.data instanceof File?event.data:event.data.file,action=event.data instanceof File?'inspect':event.data.action;send({type:'progress',stage:'opening',message:'Opening archive…'});reader=new ZipReader(new BlobReader(file));const entries=await reader.getEntries(),detection=detectArchive(entries.map(e=>({filename:e.filename,directory:e.directory,uncompressedSize:e.uncompressedSize,compressedSize:e.compressedSize})));if(action==='inspect'){send({type:'progress',stage:'inspecting',message:`Inspecting ${entries.length.toLocaleString()} files…`});if(detection.supported)send({type:'progress',stage:'detected',message:'Facebook archive detected'});send({type:'result',result:detection});return}if(!detection.supported)throw new Error('This is not a supported Facebook archive.');const candidates=entries.filter(e=>!e.directory&&e.filename.toLowerCase().endsWith('.json')&&(e.uncompressedSize??0)<=20*1024*1024),data=emptyNormalizedData();for(let index=0;index<candidates.length;index++){const entry=candidates[index],path=entry.filename.toLowerCase().replaceAll('\\','/');let section:'profile'|'posts'|'messages'|undefined;if(path.includes('profile_information')||path.includes('profile_v2'))section='profile';else if(path.includes('/posts/')||path.endsWith('your_posts.json')||path.includes('your_posts__'))section='posts';else if(/messages\/(inbox|archived_threads)\/.+\/message_\d+\.json$/.test(path))section='messages';if(!section)continue;send({type:'progress',stage:section,message:`Parsing ${section}…`,completed:index+1,total:candidates.length});try{const fileEntry=entry as typeof entry&{getData:(writer:TextWriter)=>Promise<string>},raw=JSON.parse(await fileEntry.getData(new TextWriter()));if(section==='profile')data.profile??=parseFacebookProfile(raw,entry.filename);else if(section==='posts')data.posts.push(...parseFacebookPosts(raw,entry.filename));else{const parsed=parseFacebookConversation(raw,entry.filename);if(parsed.conversation&&!data.conversations.some(c=>c.id===parsed.conversation!.id))data.conversations.push(parsed.conversation);data.messages.push(...parsed.messages)}}catch(error){data.warnings.push(`${entry.filename}: ${error instanceof Error?error.message:'Unsupported JSON'}`)}}send({type:'progress',stage:'complete',message:'Local import complete',completed:candidates.length,total:candidates.length});send({type:'import-result',data})}catch(error){send({type:'error',message:error instanceof Error?error.message:'The archive could not be processed.'})}finally{await reader?.close().catch(()=>{})}};
