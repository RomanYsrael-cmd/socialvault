import {test,expect} from '@playwright/test'; import {ZipWriter,BlobWriter,TextReader} from '@zip.js/zip.js';
test('opens a synthetic Facebook archive',async({page})=>{const writer=new ZipWriter(new BlobWriter('application/zip'));await writer.add('profile_information/profile_information.json',new TextReader('{"profile":{"name":"Synthetic User"}}'));await writer.add('your_facebook_activity/posts/your_posts__1.json',new TextReader('[]'));await writer.add('messages/inbox/synthetic/message_1.json',new TextReader('[]'));const blob=await writer.close();await page.goto('/');await page.locator('input[type=file]').setInputFiles({name:'synthetic-facebook.zip',mimeType:'application/zip',buffer:Buffer.from(await blob.arrayBuffer())});await expect(page.getByText('synthetic-facebook.zip')).toBeVisible();await page.getByRole('button',{name:/Inspect archive/}).click();await expect(page).toHaveURL(/\/archive$/,{timeout:15_000});await expect(page.getByRole('heading',{name:'Your archive is open'})).toBeVisible();await expect(page.getByText('synthetic-facebook.zip')).toBeVisible();await expect(page.getByText('Your data never left this device')).toBeVisible()});
